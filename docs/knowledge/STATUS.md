# Status

**Read this first.** Live snapshot of where the build is. Updated as acceptance criteria clear (by `/phase-check` or by hand).

- **Last updated:** 2026-06-30
- **Current focus:** **Narrative career-discovery quiz** — the single live flow after the Phase-4 strip + the Phase-5 three-role pivot (`DECISIONS.md` D-027/D-028, `DATA_MODEL.md` §17). A branching intro (6 MC: experience Q0, education Q1/Q2, salary Q3, two interest MCs Q4/Q5) then **7 day-in-the-life scenes**, each sorting its 3 choices into the 3 buckets **That's me / Kinda me / Not me** (one `SORT_BUCKETS` constant; "Kinda me" scores `MAYBE_WEIGHT` 0 — D-018) via the shared `BucketSort`. Scores ARM's **three live roles** (Technician entry / Specialist mid / Integrator planning) and lands on the dark **role-cards results** (D-029 Phase C, `Results/cards/`): a hero (match label, role name, match %, ranked signal bars), an inline "why you matched" breakdown, and two tabs (the role + an entry-Technician path-up callout; competencies + bridge programs), stepping prev/next through the ranked roles. _(The old light node-graph results were deleted in Phase E, superseded by the bubble map.)_ The education/pay fit read is folded into the breakdown's "openers" (pure `lib/screenerFit.ts`, role `educationLevel`/`payLevel`, D-019/D-020/D-023; the standalone `FitNote` banner is retired). Plus the standalone **`/select` role-select comparator** (pick a role outright from the three `roleDetails` cards, thin "You're set as [Role]" confirmation — **not** a registered flow: no FlowId, no session state, no scoring; D-022, `sessions/2026-06-11-role-select-comparator.md`). The Landing switcher reads **Narrative / Select**. Gates green: lint, typecheck, **74 unit (8 files)**, **3 E2E** (`narrative` / `role-select` / `reduced-motion`). _The **Classic** interest-sort and the **Exam** 30-statement flow were the study's comparison conditions; the narrative won (June study) and both were archived in Phase 4, recoverable at git tag `archive/pre-narrative-only`._ **Quiz-fidelity pass (2026-06-29, post-Phase-B, before Phase C):** three interactions brought in line with the Claude Design mockup — the scene **slide-up morph** on Continue, every step's **horizontal slide** with the **selected row holding gold through the exit** ("stays lit"), and a persistent branch-aware **Back** (prior picks pre-lit + re-pickable; the research-flagged "no way back" gap). The runner cursor moved into the store (`scenePhase`/`choiceIndex` + a `history` back-stack; new actions `startScene`/`rateChoice`/`goBack`); `BucketSort` now has an inner per-choice `ChoiceCard`. All gates green (lint, typecheck, 49 unit, 3 E2E, zero console warnings). Handoff `sessions/2026-06-29-quiz-fidelity-back-slide-morph.md`. _A fresh motion look is still owed at Phase G (noted in `VISUAL_REARCHITECTURE.md` §6 Phase G)._ Session handoffs: `sessions/2026-06-30-phase-F-constellation-job-overview.md` (this — the job constellation + job overview, the last of the 5 results screens), `sessions/2026-06-30-phase-E-bubble-map.md` (the bubble map), `sessions/2026-06-30-phase-D-results-compare.md` (the compare screen), `sessions/2026-06-29-phase-C-results-role-cards.md` (the role cards), `sessions/2026-06-26-realignment-completion-audit.md` (completion audit + doc-drift cleanup — read alongside the pivot note before step 8), `sessions/2026-06-26-phase-5-three-roles-executed.md` (this pivot), `sessions/2026-06-25-phase-4-strip-executed.md`, `sessions/2026-06-25-strip-to-narrative-and-three-roles.md` (the Phase 4/5 plan), `sessions/2026-06-22-narrative-v3-language-and-intro-scoring.md`.
- **Phase 1 status:** complete — all gates green; design-review passed all four classic screens with no p1/p2 findings. Built in 3 slices: Sort, Results (the conversion screen), Landing reveal + Build beat.
- **Open items (for step 8, the high-fidelity results screen):** define what the match percentage means in one plain line (the #1 content gap from testing); frame an entry-level result as a starting rung with a visible path up, not a verdict; wire match-explanation onto the narrative (`categoryBreakdown.ts` is kept, currently unwired); re-introduce a **category-keyed** programs set (the live results surface zero programs today — the known "somewhere to go" gap, since `programs`/`programSelection` were deleted in Phase 4); redesign the Landing hero (frees the `scene/*` token removal) and retone `arm-blue`. Then a graded `/design-review` against `results-screen.md`. The on-canvas title scatter was simplified to a tray (collision) and still wants a polish pass.
- **Next up:** **Step 8** — the **dark visual re-architecture** of the whole quiz (re-skin matching ARM's live **My Match** flow + the mockup's **5-screen results system**). Plan of record: **`docs/knowledge/VISUAL_REARCHITECTURE.md`** (7 short phases 0/A–G; `DECISIONS.md` D-029). **Phase 0 (plan landing) and Phase A (dark token foundation + local fonts + `AppHeader` shell + dark type-led Landing) are DONE** — gates green (lint, typecheck, 49 unit, 3 E2E, build), graded `/design-review` clean (zero p1/p2, AA verified on every dark pairing), handoff `sessions/2026-06-26-phase-A-dark-foundation.md`. **Phase B (quiz re-skin) is DONE** — the intro MC (`MCQuestion`) renders as a dark glass question card with branch-aware "Question N of M", optional prompt, and gold auto-advancing answer rows; each of the 7 scenes (`SceneSortView` + the rewritten `BucketSort`) runs the **two-beat** (a scene-context card "Scene N of 7" + gold **Continue**, then the three choices one at a time as rating cards sliding in "Choice N of 3", each rated into the three buckets via **uniform gold rating rows** — D-018 labels kept). Confirmed calls (this session): keep the two-beat, uniform gold rows (Caelan over my semantic-tint rec). Drag (`DragSortCard`/`DropZone`) left dormant (rule 4). Gates green (lint, typecheck, 49 unit, **3 E2E** with the narrative/reduced-motion specs updated to the two-beat — narrative bumped to a 60s timeout for the longer flow, build); graded `/design-review` **PASS** (one p2 focus-ring + p3 polish, all fixed: tokenized off-white `*:focus-visible`, new `--radius-lg` 16px card token, reduced-motion crossfade pinned to `durations.snap`, misleading `max-w-xl` dropped from the cards, numbering made branch-aware so the Q1=No branch no longer skips a number). Post-review the choice-rating card was un-nested (prompt as its own card, standalone rating rows below — mirrors the intro pattern) to match the mockup. The compliance rubric was refreshed **v2 → v3** for the dark system (retired `light-mode-only`/`no-glassmorphism` → `dark-on-system`/`glass-from-tokens`; gold/teal/orange accents; scene-token clause dropped; GSAP-zero-live-uses). **Deferred to Phase G (a11y):** the `BucketSort` slide's `overflow-hidden` clips the L/R edges of the rating-row focus ring (legible, not broken — documented at the site); and the quiz reading column wants a real container token instead of `max-w-2xl`. **Owed at Phase C:** extend `results-screen.md` for the dark 5-screen results. Handoff `sessions/2026-06-26-phase-B-quiz-reskin.md`. **Phase C (results role cards) is DONE** — `/results` re-architected from the light node map to the mockup's dark **role-cards** screen (`Results/cards/`): `ResultsPanel` (sticky glass control bar, Compare/Map stubbed for D/E on a `useResultsNav` seam) → `RoleHero` (neutral role name + match %, ranked **signal bars** replacing the triangle radar, prev/next arrows) → inline **`WhyYouMatched`** (collapsed "where your X% comes from" → expanded 01/02/03 + "what you passed on", wiring the now-live `categoryBreakdown` with an openers/moments split + `passedLabels`) → `RoleTabs` (description + entry-Technician **path-up** callout + salary/education + `{heading,text}` duties / real ARM **competencies** chips + placeholder **bridge programs**). The three step-8 content goals all landed (match-% line, Technician-as-rung, breakdown provenance). Node map / `FitNote` dormant on disk (Phase E); `RoleDetailSheet`+`FitRadar` still live in `/select`. Gates green: lint, typecheck, **55 unit**, **3 E2E** (narrative/reduced-motion updated to the cards DOM), build, zero console errors. Graded `/design-review`: `design-system-compliance` PASS, `results-screen` 1 p1 + 1 p2 **fixed** (technician path-up; 0-moment breakdown no longer asserts "you kept…"), 1 p3 (signal-bar graphical contrast, mitigated) **deferred to Phase G**. Handoff `sessions/2026-06-29-phase-C-results-role-cards.md`. **Phase D (results compare) is DONE** — the Compare control now opens a real `CompareView` (`Results/cards/`): the current role (left) side-by-side with a switchable target (right) via a `CompareTargetMenu` dropdown, each `CompareColumn` reusing the Phase C hero/`SignalBars`/`WhyYouMatched`/`StatBox` + description + duties (no path-up callout), plus a soft **"Our take"** recommendation line. **Calls (this session, AskUserQuestion):** the canonical mockup compare has **no swap and no recommendation line** (dead "compare-pick" code) — we kept "no swap" but **added a recommendation line** with a **"foreground the lower barrier when close"** framing (new pure `lib/compareRecommendation.ts`, +6 tests; copy in `resultsCopy.cards.recommendation`, **signed off by Caelan 2026-06-30 — final**); responsive = desktop-first, columns stack under `md`. `useResultsNav` gained `compareWith` + per-column `compareExpanded`. Gates green: lint, typecheck, **61 unit**, **3 E2E** (narrative rewritten to the compare DOM), build, zero console errors. Graded `/design-review` **PASS** both rubrics (2 p2 fixed: dropdown check now per-row accent, magic-px tokenized; 2 p3 deferred to Phase F/G: outbound next-step from compare, rung-on-compare). **Post-review polish (same day, Caelan live):** the **real RC TopNav** ported from the dashboard repo replaced the Phase-A placeholder (60px solid, real logo, centered search, gold profile pill; new `--spacing-nav`/`--color-gold-deep`/`sliders` icon); the results sheet is now a **viewport-height scroll container + sticky glass header** (top pinned a constant gap below the nav, body scrolls under the glass, bottom revealed only at the end with a matching gap, corners clipped clean); "Our take" moved to the top of compare; the compare dropdown trigger got the accent dot; **salary standardized to the national median** (`RoleDetail.salaryMedian`) for even stat-box height; "Start over" → **"Retake quiz"**; and a **dev-only "skip to results"** control (`devSeedResults()`, `import.meta.env.DEV`) on Landing for fast iteration (**remove at Phase G**). Gates re-green (61 unit/3 E2E/build). Handoff `sessions/2026-06-30-phase-D-results-compare.md`. **Phase E (results bubble map) is DONE (2026-06-30)** — the stubbed `map` view is now the real ambient **bubble map** (`Results/cards/ResultsMap` + `BubbleField` + `AmbientField`): a full-bleed dark canvas (the `<main>` relaxes its rounded panel for `map`), a decorative reduced-motion-gated `AmbientField` (six blurred role-tinted orbs), a glass "Your results" intro card, and the three roles as **bubbles sized by match %** (rank-based via pure `lib/bubbleLayout`; top match largest/centred). Tapping a bubble **dives** into that role's cards (`useResultsNav.diveToRole` sets `roleIndex` + `fromMap`); the cards control bar then shows **"Back to the map"**. The **node map's fate is resolved: deleted** (`CategoryResults` / `NodeMap` / `FitNote`; `FitRadar` + `RoleDetailSheet` + `nodeLayout.ts` stay for `/select`). Locked calls (AskUserQuestion): scope = Phase E only, layout = rank-based, delete the node-map. Gates green: lint, typecheck, **65 unit** (+4 `bubbleLayout`; `data-integrity` copy-walk extended for the `map` block), **3 E2E** (`narrative`/`reduced-motion` open the map → dive into the top match → back), build, zero console errors. Graded `/design-review` **PASS on both rubrics, zero p1/p2** — only token-hygiene polish, fixed this session (idle-loop easings → `easings.soft`; off-scale bubble text + decorative literals marked intentional). Handoff `sessions/2026-06-30-phase-E-bubble-map.md`. **Phase F (results constellation + job overview) is DONE (2026-06-30)** — screens 6 + 7 of the mockup, so **all five results screens are now built**. The map bubble now dives into a role's **job constellation** (`Results/cards/ResultsConstellation` + `ConstellationField` + `ConstellationNode` + `JobSidePanel`): the role at the center, its featured jobs (`src/data/jobs.ts`, 3/5/5, **placeholder** copy) ringing it on a count-aware polar layout (pure `lib/constellationLayout`, +7 tests), dashed center→node edges, a 404px glass side panel (role summary + "Jobs in this path" + "Role overview" → cards). A node opens the **job overlay** (`view: 'job'`; the clicked node fills+glows, others dim, the panel body crossfades to the job summary; `selected`+`job` share one mounted shell so the constellation never remounts). The gold "Job overview" footer opens the standalone **`JobOverview`** page (reuses `ResultsPanel`; local-state 3 tabs: overview / skills+competencies+bridge programs / "how you fit" + a static `TrajectoryViz` Technician→Specialist→Integrator ladder; inert "Set as target role"). `useResultsNav` gained `selectedJob` + `openConstellation`/`openJob`/`openJobOverview`/`backToConstellation`/`backToJob`/`roleOverview` (and dropped `diveToRole`). Locked calls (AskUserQuestion): distinct per-job placeholder (not the mockup's shared text), bubble→constellation (revises Phase E), count-aware 3/5/5. New `Job` type + `ResultsExploreCopy`; `data-integrity` extended (explore copy-walk, `overviewTabs`, per-job 3/5/5 invariants); `EducationList` extracted (shared by the role tab / side panel / overview); `--container-constellation`/`--container-job-panel` tokens. Gates green: lint, typecheck, **74 unit**, **3 E2E** (narrative walks bubble → constellation → node → job overlay → job overview → 3 tabs → back chain → Role overview → cards → retake; reduced-motion dives + opens a job with normal clicks), build, zero console errors. Graded `/design-review` **PASS on both rubrics, zero p1**; 1 p2 **fixed** (inactive node-star glyph → `accent.textSoft` to clear the 3:1 graphical-contrast bar), 3 p3 deferred to Phase G. Handoff `sessions/2026-06-30-phase-F-constellation-job-overview.md`. **Phase G (polish, against the Claude Design `.dc.html` as ground truth) is DONE (2026-06-30)** — completed the prior session's ~70%: the broken E2E assertion fixed (`narrative.spec.ts` `back-to-map` → `explore-role`); the **weird quiz-intro motion** fixed (intro MC steps **top-anchor** via `FlowRunner` `justify-start pt-space-7` so different-height questions no longer vertical-jump on swap, scenes stay centered so the liked slide-up morph is untouched — verified: card top stable at 124px across 2/4-choice questions); the **constellation** star glow strengthened (layered `drop-shadow`) and non-dimmed node labels tinted `accent.textSoft` (one-role-constellation read); the **trajectory** "where this can lead" rebuilt to **centered labels** (senior above, branches+current below) fixing a real side-pill clipping bug, long placeholder names shortened; **why-matched chips** confirmed one-line (`maxChars:40,maxCount:2`); and the design-review p3s addressed — **job-overview header** now carries a `RoleDetail.tierLabel` **level pill** ("Entry level/Mid level/Planning") with the title bumped `text-h3`→`text-h2` (reference parity), placeholder `specialist[0]` "Robotics Specialist" → **"Robotics Programmer"** (so the constellation doesn't read role==job). Gates green: lint, typecheck, **80 unit**, **3 E2E**, build. Graded `/design-review` **PASS on both rubrics, zero p1/p2** (remaining p3s fixed or deliberately left: the trajectory's decorative handoff literals; the latent gold=Technician/gold=my-pick watch). **Deferred (not from this pass):** `technician-is-a-rung` (p1) + `low-signal-graceful` (p3) couldn't be graded from a Specialist-top capture — structurally supported (entry `pathUp`, trajectory, bridge programs) but want a Technician-top review; the **responsive story** for the desktop-first constellation/map; the dev-only "skip to results" control still present (DEV-only, never ships). Handoff `sessions/2026-06-30-phase-G-polish-complete.md`. _Step 8 is complete._ Locked calls still standing: kit-only role palette **gold(Technician)/teal(Specialist)/orange(Integrator)**, `arm-blue` **retired**; our question set is ground truth; local dark tokens now, shared package deferred. **Doc-sync done (this session, via `/revise-doc` + `doc-steward`):** `DESIGN_SYSTEM.md` §3.5 records the dark layer (ramp + off-white text + glass/blur + role accents), §3.3 the finalized gold/teal/orange palette, `arm-blue` retired, fonts self-hosted; `CLAUDE.md` stack table marks GSAP zero live uses + drops the deleted `/scene`; the steward reconciled `ARCHITECTURE`/`PRD`/`ROADMAP`/`DATA_MODEL` (and fixed the now-contradictory "dark mode / glassmorphism out of scope" lines). `lib/gsap.ts` still registers at app start as a future seam. The open items above are folded into the plan (Phase C lands the match-% line, the Technician-as-rung framing, the `categoryBreakdown` wiring, and the programs set; the 3-axis radar is replaced by signal bars). Per-role job + bridge-program content will be **ARM-sourced** via the data-request template (`docs/reference/Job_Program_Data_Request.md`). The old conveyor "feel pass" stays **superseded**. The realignment sweep is complete: foundation slice (D-024), Phase 2 spec re-centering (D-025), Phase 3 gate rewrite (D-026), **Phase 4 strip to narrative (D-027)**, and **Phase 5 — collapse to ARM's three roles (D-028, done):** Operate+Repair → entry **Technician** ($45,936, HS/GED), Program → **Specialist**, Plan → **Integrator**; `CategoryId` renamed `technician/specialist/integrator`; the salary question rebuilt on the real ARM ranges ($45k → technician, $85k/$105k+ → specialist+integrator) with "1-2 years" left unscored so the per-role maxes stay equal at `{11,11,11}`; diamond node-map/radar flipped to a triangle (`CATEGORY_ANGLES` + `FitRadar`); `StatementSortStep`/exam-`kind` removed; `CLAUDE.md` hard rule rewritten + D-017 carve-out retired; `verifier`/`data-author` swept to three roles. Branch `narrative-v3-realign`, all gates green. _(The dead `howler`/`@types/howler` devDeps were trimmed in the D-028 pass; `audio.ts` is gone.)_

---

## Harness bootstrap (this session)

- [x] Reconcile cross-doc contradictions (see `DECISIONS.md` 2026-05-29).
- [x] Onboarding substrate: `.mcp.json`, `.env.example`, `.gitignore`, root `README.md`, `.nvmrc`.
- [x] Knowledge layer scaffolded (`docs/knowledge/`).
- [x] Rubrics authored (`docs/rubrics/`: design-system-compliance, goose-game-aesthetic, motion-quality). _(Point-in-time log — `goose-game-aesthetic` + `motion-quality` were later retired in D-026 and `results-screen` added; the current set is `design-system-compliance`, `results-screen`.)_
- [x] Subagents (`.claude/agents/`: verifier, design-reviewer).
- [x] Skills (`.claude/skills/`: data-author, scene-motion).
- [x] Slash commands (`.claude/commands/`: phase-check, design-review, compound, capture-figma, pull-figma). _(`revise-doc` was ported later in the realignment, D-024.)_
- [x] `.claude/settings.json` permission allowlist.
- [x] `CLAUDE.md` + `ARCHITECTURE.md` updated for the harness.
- [x] Toolchain skills installed — GSAP + Framer Motion skills present (stored in `.agents/skills/`, symlinked into `.claude/skills/`, pinned in `skills-lock.json`).
- [x] Built-in memory mirror written.

---

## Phase 0 — Scaffold & foundation (complete — 10/10)

_Last `/phase-check`: 2026-05-29 — `verifier` PASS on all gates (lint, typecheck, 30 unit tests, e2e happy-path) and all `DATA_MODEL.md` §15 invariants; re-confirmed post-commit on branch `phase-0-scaffold`. Tailwind v4 (CSS-first `@theme`) adopted instead of v3 — see `DECISIONS.md` D-013. Also fixed the stale ROADMAP §1.4 "Innovator=24" typo → 27 (matches §15 + committed data)._

Acceptance criteria (from `ROADMAP.md` §1):

- [x] `pnpm dev` runs without errors.
- [x] `pnpm lint` passes.
- [x] `pnpm typecheck` passes.
- [x] `pnpm test:unit` passes scoring, assembly, program-selection tests (30 tests, incl. `data-integrity`).
- [x] `pnpm test:e2e` passes the happy-path Playwright test.
- [x] A human can click `/` → `/results` with no console error (e2e asserts zero console errors across the flow).
- [x] Tailwind tokens match the Figma file's variables (spot-check). Brand colors (arm-yellow `#FFB81C`, arm-orange `#F56A00`, arm-blue `#38A5EE`, arm-teal `#117289`) and the type scale (h1 56/64 … small 14/22) in `@theme` match `DESIGN_SYSTEM.md` §2–§4, which the file owner confirms were pulled from the Design System file's variables. _(A live MCP variable read was attempted but blocked — the `figma` MCP's variable/context tools are selection-based and `get_metadata` served a stale page snapshot; this is a tooling limit, not a value discrepancy.)_
- [x] Figma MCP connects, a read works, one code-to-canvas capture succeeds (Landing captured into the `explore_floor` file — node `6:2`).
- [x] GSAP AI skills installed and discoverable (`gsap-*` packs in `.claude/skills/`, pinned in `skills-lock.json`).
- [x] Every data sanity check from `DATA_MODEL.md` §15 passes (per-archetype sums **B 22 / I 27 / A 25**).

## Phase 1 — Testable flow (complete)

_Last `/phase-check`: 2026-05-30 — `verifier` PASS on all gates (lint, typecheck, 33 unit across scoring/assembly/program/fit/data-integrity, 4 E2E) and all `DATA_MODEL.md` §15 invariants (sums recomputed Builder 22 / Innovator 27 / Architect 25, plus the new role.skillIds-resolves check). `design-reviewer` PASS on all four screens — no p1/p2 findings. Built in 3 reviewed slices on branch `phase-1-flow`._

Acceptance criteria (from `ROADMAP.md` §2):

- [x] A user can complete the full flow (land → sort 24 → build → results). _(Flow completes end to end; the 3-4 min pacing is a human-test observation.)_
- [x] The Sort screen works via mouse, touch, and keyboard. _(Drag + tap; bins are native buttons so Tab/Enter operability remains. The bespoke arrow-key/Enter mechanic was intentionally removed — `DECISIONS.md` D-015. Full keyboard-nav polish is a Phase 3 a11y item.)_
- [x] Results shows believable weighted match scores across all three archetypes. _(E2E asserts displayed % == engine for a 30/24/23 spread.)_
- [x] The compare interaction works (moving the robot / tapping a card swaps the active read). _(E2E `compare.spec.ts`.)_
- [x] Each role card surfaces programs from the mock data. _(top-3 via `selectProgramsForRole`.)_
- [x] All Playwright tests pass, including the compare interaction. _(4/4.)_
- [x] No console errors on any screen. _(happy-path + reduced-motion specs assert zero console errors across the flow.)_
- [ ] **Demoable to the MHCI cohort; survives 5 unmoderated sessions.** _(Human user-test item — for the team to confirm in testing. Build is technically demoable: flow completes, no crashes in automated runs.)_

## Phase 2 — Feel pass (not started)
Criteria in `ROADMAP.md` §3. Sort interaction model refined — see `DECISIONS.md` D-014.

## Phase 3 — Polish (not started)
Criteria in `ROADMAP.md` §4.
